# Permission Schemas

Defines the **canonical JSON schema** for representing SQL Server permissions.

Schemas ensure:
- Consistent structure across environments
- Deterministic validation
- Diff-friendly change tracking

Used by both demo data and enterprise snapshots.
