# SQL Scripts

This directory contains read-only SQL scripts used to generate permission
snapshots from SQL Server environments.

Scripts are designed to be:
- Non-invasive
- Auditable
- Safe to run in production
